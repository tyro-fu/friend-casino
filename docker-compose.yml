services:
  poker:
    build: .
    ports:
      - "3001:3000"
    environment:
      - PORT=3000
      - DATA_DIR=/app/data
    volumes:
      - poker_data:/app/data
      - ./config:/app/config:ro
volumes:
  poker_data:
